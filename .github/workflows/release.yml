# Build and publish Electron app to GitHub Releases (public repo ClipCast-Releases) on version tags.
# Usage: bump version in package.json, commit, then push tag (e.g. v0.1.1).
# GH_TOKEN: PAT with repo scope, stored in this (private) repo's Actions secrets; used only in CI, never shipped in the app.
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    env:
      VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Verify Vite env (no secrets)
        shell: pwsh
        run: |
          $hasUrl = [string]::IsNullOrWhiteSpace($env:VITE_SUPABASE_URL) -eq $false
          $hasKey = [string]::IsNullOrWhiteSpace($env:VITE_SUPABASE_ANON_KEY) -eq $false
          Write-Host "VITE_SUPABASE_URL is set: $hasUrl"
          Write-Host "VITE_SUPABASE_ANON_KEY is set: $hasKey"

      - name: Prepare bundled Python runtime
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $pyVersion = "3.12.10"
          $pyZipUrl = "https://www.python.org/ftp/python/$pyVersion/python-$pyVersion-embed-amd64.zip"
          $getPipUrl = "https://bootstrap.pypa.io/get-pip.py"
          $zipPath = Join-Path $env:RUNNER_TEMP "python-embed.zip"
          $extractPath = "vendor\python"
          $getPipPath = Join-Path $env:RUNNER_TEMP "get-pip.py"
          Write-Host "==> Download embeddable Python $pyVersion"
          Invoke-WebRequest -Uri $pyZipUrl -OutFile $zipPath
          if (Test-Path $extractPath) { Remove-Item -Recurse -Force $extractPath }
          New-Item -ItemType Directory -Force -Path $extractPath | Out-Null
          Write-Host "==> Extract embeddable Python"
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
          $pythonExe = Join-Path $extractPath "python.exe"
          if (-not (Test-Path $pythonExe)) { throw "python.exe missing after extract: $pythonExe" }
          Write-Host "==> Enable site-packages"
          $pthFile = Join-Path $extractPath "python312._pth"
          if (-not (Test-Path $pthFile)) { throw "python312._pth not found: $pthFile" }
          (Get-Content $pthFile) -replace '^#import site','import site' | Set-Content $pthFile -Encoding ASCII
          Write-Host "==> Install pip into embedded Python"
          Invoke-WebRequest -Uri $getPipUrl -OutFile $getPipPath
          & $pythonExe $getPipPath
          Write-Host "==> Upgrade pip"
          & $pythonExe -m pip install --upgrade pip
          Write-Host "==> Install requirements into embedded Python"
          & $pythonExe -m pip install -r yt_pipeline/requirements.txt
          Write-Host "==> Verify embedded runtime"
          & $pythonExe -c "import sys; print(sys.executable)"
          if (-not (Test-Path $extractPath)) { throw "Missing vendor/python" }
          if (-not (Test-Path (Join-Path $extractPath 'python.exe'))) { throw "Missing vendor/python/python.exe" }
          if (-not (Test-Path (Join-Path $extractPath 'python312._pth'))) { throw "Missing vendor/python/python312._pth" }

      - name: Prepare bundled ffmpeg/ffprobe
        shell: pwsh
        run: |
          $ffmpegUrl = "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip"
          $zipPath = Join-Path $env:RUNNER_TEMP "ffmpeg.zip"
          $extractPath = Join-Path $env:RUNNER_TEMP "ffmpeg"
          Invoke-WebRequest -Uri $ffmpegUrl -OutFile $zipPath
          if (Test-Path $extractPath) { Remove-Item -Recurse -Force $extractPath }
          Expand-Archive -Path $zipPath -DestinationPath $extractPath -Force
          $rootDir = Get-ChildItem -Path $extractPath -Directory | Select-Object -First 1
          if (-not $rootDir) { throw "ffmpeg archive root folder not found" }
          $binDir = Join-Path $rootDir.FullName "bin"
          if (-not (Test-Path $binDir)) { throw "ffmpeg bin folder not found: $binDir" }
          New-Item -ItemType Directory -Force -Path vendor\bin | Out-Null
          Copy-Item (Join-Path $binDir "ffmpeg.exe") -Destination vendor\bin\ffmpeg.exe -Force
          Copy-Item (Join-Path $binDir "ffprobe.exe") -Destination vendor\bin\ffprobe.exe -Force
          if (-not (Test-Path vendor\bin\ffmpeg.exe)) { throw "ffmpeg.exe missing after copy" }
          if (-not (Test-Path vendor\bin\ffprobe.exe)) { throw "ffprobe.exe missing after copy" }

      - name: Setup Miniconda (CUDA DLLs)
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniconda-version: "latest"
          auto-activate-base: false

      - name: Install CUDA runtime DLLs (Conda)
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $envName = "cuda-dlls"
          Write-Host "==> Create conda env: $envName"
          try {
            conda create -y -n $envName -c nvidia -c conda-forge cuda-cudart libcublas
          } catch {
            throw "Conda env creation failed for $envName (cuda-cudart/libcublas)."
          }
          Write-Host "==> Attempt to install cuDNN (optional)"
          try {
            conda install -y -n $envName -c nvidia -c conda-forge cudnn
          } catch {
            try {
              conda install -y -n $envName -c nvidia -c conda-forge libcudnn
            } catch {
              Write-Host "cudnn/libcudnn install failed or unavailable; continuing"
            }
          }
          $envs = conda env list --json | ConvertFrom-Json
          $envPath = $envs.envs | Where-Object { $_ -match "\\$envName$" } | Select-Object -First 1
          if (-not $envPath) { throw "Conda env path for $envName not found" }
          "CONDA_PREFIX=$envPath" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding ascii
          Write-Host "CONDA_PREFIX set to $envPath"
          Write-Host "==> Conda packages installed in $envName"
          conda list -n $envName
          Write-Host "==> DLLs in $envPath\\Library\\bin"
          Get-ChildItem -Path (Join-Path $envPath "Library\\bin") -Filter "*.dll" | Select-Object -ExpandProperty Name
          $requiredDlls = @("cublas64_12.dll","cublasLt64_12.dll","cudart64_12.dll")
          $missing = @()
          foreach ($dll in $requiredDlls) {
            $dllPath = Join-Path $envPath "Library\\bin\\$dll"
            if (-not (Test-Path $dllPath)) { $missing += $dll }
          }
          if ($missing.Count -gt 0) {
            throw ("Missing required CUDA DLLs in conda env: " + ($missing -join ", "))
          }
          $cudnnPath = Join-Path $envPath "Library\\bin\\cudnn64_8.dll"
          if (Test-Path $cudnnPath) {
            Write-Host "Found cudnn64_8.dll"
          } else {
            Write-Host "cudnn64_8.dll not found (optional)"
          }

      - name: Build
        run: npm run build

      - name: Release (electron-builder)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npm run release
